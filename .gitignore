# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables — never commit real secrets
.env
.env.*
!.env.example
!.env.local.example
.env*.local
.env*.tmp

# Secrets / keys — never commit private keys or credential dumps
*.pem
*.key
id_rsa*
id_ed25519*
*_rsa
/d
.npmrc.local

# Common ignores
node_modules
.next/
.DS_Store
.vercel

# Sentry Config File
.env.sentry-build-plugin
HANDOFF.md

# Capacitor — web assets are rebuilt on sync, don't commit
ios/App/App/public
